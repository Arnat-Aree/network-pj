apiVersion: apps/v1
kind: Deployment
metadata:
  name: nti-api
  labels:
    app: nti-api
spec:
  replicas: 2
  selector:
    matchLabels:
      app: nti-api
  template:
    metadata:
      labels:
        app: nti-api
    spec:
      serviceAccountName: nti-api-sa
      containers:
        - name: nti-api
          image: network-pj/backend-api:latest
          envFrom:
            - secretRef:
                name: nti-secrets
          ports:
            - containerPort: 8000
          readinessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 10
            periodSeconds: 10

---

apiVersion: v1
kind: Service
metadata:
  name: nti-api
spec:
  selector:
    app: nti-api
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
  type: ClusterIP
