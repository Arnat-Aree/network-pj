Helm chart skeleton

This folder contains a minimal Helm chart skeleton for production deployment. It is a starting point — adjust values, templates, and add Ingress/ServiceMonitor/Secrets as needed.

Structure:
- charts/
- templates/
- values.yaml

*** End Patch
