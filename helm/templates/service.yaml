apiVersion: v1
kind: Service
metadata:
  name: {{ include "network-pj.fullname" . }}-svc
spec:
  selector:
    app: {{ include "network-pj.fullname" . }}
  ports:
    - protocol: TCP
      port: {{ .Values.service.port }}
      targetPort: 8000
  type: {{ .Values.service.type }}
